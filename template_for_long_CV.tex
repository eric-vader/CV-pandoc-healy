\documentclass[11pt, a4paper]{article}

% This is a tex file to make Ben Schmidt's CV.
% The content is 

\usepackage{jk-vita}
% \usepackage[notes,natbib,isbn=false,backend=biber,url=false,numbermonth=true]{biblatex-chicago}


% \usepackage{biblatex}

% Your biblatex file is likely somewhere else.
$if(bibliography)$
\addbibresource{$bibliography$}
$endif$

\title{$title$}
\name{$name$}
% I guess postnoms is like junior? I dunno.
\postnoms{}
\address{$address$}
\www{$www$}
\email{$email$}
\tel{$tel$}
\twitter{$twitter$}
\github{$github$}
\subject{}


\begin{document}

\maketitle

\section{experience}
$for(appointment)$
\subsection{$appointment.place$}
\begin{separator}
  $for(appointment.items)$
    \entry{$appointment.items.dates$}{\textit{$appointment.items.item$}
      $if(appointment.items.description)$
        \\\justifying{\noindent\small $appointment.items.description$}
      $endif$
    }\\
    \vspace{.75ex}
  $endfor$
\end{separator}
$endfor$

% /allbutlast

% $for(appointment.items/last)$
% \entry{$appointment.items.dates$}{\textit{$appointment.items.item$}
%   $if(appointment.items.description)$
%     \\\justifying{\noindent\small $appointment.items.description$}
%   $endif$
% }
% $endfor$

\section{education}
% Education uses a different field called ``items.''
$for(education)$
\subsection{$education.item$, $education.place$}
\begin{separator}
  \entry{$education.date$}{\textit{$education.grade$}
    $if(education.info)$
      \\\justifying{\noindent\small
      $for(education.info)$
        $if(education.info.text)$
          % Store certain things in a field called 'text' to allow another field
          % for priority.
          $education.info.text$
        $else$
          $education.info$
        $endif$
      $endfor$}
    $endif$
  }\\
  \vspace{.75ex}
\end{separator}
$endfor$

\section{achievement}
\subsection{Awards}
\begin{separator}
$for(awards)$
  \entry{$awards.date$}{\textit{$awards.item$, $awards.place$}
    \\\justifying{\noindent\small $awards.info$}
  }\\
  \vspace{.75ex}
$endfor$
\end{separator}
\subsection{Scholarships}
\begin{separator}
$for(scholarships)$
  \entry{$scholarships.date$}{\textit{$scholarships.item$, $scholarships.place$}
    \\\justifying{\noindent\small $scholarships.info$}
  }\\
  \vspace{.75ex}
$endfor$
\end{separator}

% A little wonky. I want my publications divided up based on tags.
% (Yes, it would be better to just do this with bib file). That's for later.

\section{publications}

\subsection{Peer-Reviewed Conference Publications}
\begin{separator}
$for(publication)$
$if(publication.academic)$
  $if(publication.citekey)$
    (\cite{$publication.citekey$}).\\[.15cm]
  $else$
    \entry{$publication.date$}{\textit{$publication.title$} 
      \\\justifying{\noindent\small $publication.author$\\ $publication.journal$
      $if(publication.summary)$
        \\ $publication.summary$ 
      $endif$}
    }\\
    \vspace{.75ex}
  $endif$
$endif$
$endfor$
\end{separator}


\section{service}
$for(service)$
  \subsection{$service.type$}
  \begin{separator}
  $for(service.gigs)$
    \entry{$service.gigs.date$}{\textit{$service.gigs.item$}
      $if(service.gigs.info)$
        \\\justifying{\noindent\small $service.gigs.info$ }
      $endif$
    }\\
    \vspace{.75ex}
  $endfor$
  \end{separator}
$endfor$

\section{invited talks}

$for(invited_talk)$
``$invited_talk.title$.'' $if(invited_talk.host)$$invited_talk.host$, $endif$$invited_talk.place$. $invited_talk.date$.\\[.15cm]
$endfor$


\section{workshops}

$for(workshop)$
``$workshop.title$.''
$if(workshop.host)$$workshop.host$, $endif$$workshop.place$.
$workshop.date$.\\[.15cm]
$endfor$

\section{conference talks}

$for(conference)$
``$conference.title$.''
$if(conference.host)$$conference.host$, $endif$
$conference.place$.
$conference.date$.\\[.15cm]
$endfor$

\section{teaching}

$for(teaching)$
``$teaching.title$.'' $teaching.date$. ($teaching.type$)\\[.15cm]
$endfor$


\section{public history}

$for(public_history)$
% This one has a hierarchical setup: there are multiple roles in public history, and
% each can have any number of ``gigs''
% These are irregular enough that I just use an ``item'' field to store the text.
\subsection{$public_history.role$}
$for(public_history.gigs)$
$public_history.gigs.item$ $public_history.gigs.date$.\\[.15cm]
$endfor$
$endfor$
\subsection{Selected media coverage}

$for(media_coverage)$
$if(media_coverage.citekey)$
\fullcite{$media_coverage.citekey$}\\
$else$
$media_coverage.title$
$endif$
$endfor$




\section{research competencies}
$for(competencies)$
\subsection{$competencies.type$}
$for(competencies.items)$
$competencies.items$\\[.15cm]
$endfor$
$endfor$

\end{document}
